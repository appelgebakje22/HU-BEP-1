plugins {
    id "java"
    id "application"
    id "org.openjfx.javafxplugin" version "0.0.8"
}

group "nl.appelgebakje22"
version "0.0.1-SNAPSHOT"

javafx {
    version = "13"
    modules = [ "javafx.controls", "javafx.fxml" ]
}

repositories {
    mavenCentral()
}

dependencies {
}